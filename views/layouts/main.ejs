<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>candyskin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        :root {
            --bg-dark: #1a1a1a;
            --bg-darker: #141414;
            --accent-color: #8c52ff;
            --text-primary: #ffffff;
            --text-secondary: #b3b3b3;
            --text-muted: #6c757d;
            --card-bg: #242424;
            --card-border: #333333;
            --danger-color: #dc3545;
        }

        body {
            background-color: var(--bg-dark);
            color: var(--text-primary);
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        }

        .navbar {
            background-color: var(--bg-darker) !important;
            border-bottom: 1px solid var(--card-border);
        }

        .card {
            background-color: var(--card-bg);
            border: 1px solid var(--card-border);
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .card-header {
            background-color: var(--bg-darker);
            border-bottom: 1px solid var(--card-border);
            font-weight: 600;
            padding: 1rem 1.25rem;
            color: var(--text-primary);
        }

        .card-body {
            color: var(--text-secondary);
        }

        .btn-primary {
            background-color: var(--accent-color);
            border: none;
            padding: 0.5rem 1.5rem;
            border-radius: 6px;
        }

        .btn-primary:hover {
            background-color: #7340d8;
        }

        .btn-outline-primary {
            color: var(--accent-color);
            border-color: var(--accent-color);
        }

        .btn-outline-primary:hover {
            background-color: var(--accent-color);
            border-color: var(--accent-color);
        }

        .form-control {
            background-color: var(--bg-darker);
            border: 1px solid var(--card-border);
            color: var(--text-primary);
        }

        .form-control::placeholder {
            color: var(--text-secondary) !important;
            opacity: 0.7;
        }

        .form-control:read-only {
            color: var(--text-secondary);
        }

        .form-control:focus {
            background-color: var(--bg-darker);
            border-color: var(--accent-color);
            color: var(--text-primary);
            box-shadow: 0 0 0 0.25rem rgba(140, 82, 255, 0.25);
        }

        .form-select {
            background-color: var(--bg-darker);
            border: 1px solid var(--card-border);
            color: var(--text-primary);
        }

        .form-select:focus {
            background-color: var(--bg-darker);
            border-color: var(--accent-color);
            color: var(--text-primary);
        }

        .danger-zone {
            background-color: rgba(220, 53, 69, 0.1);
            border: 1px solid var(--danger-color);
            border-radius: 10px;
            padding: 1.5rem;
            margin-top: 2rem;
        }

        .nav-link {
            color: var(--text-secondary);
        }

        .nav-link:hover {
            color: var(--text-primary);
        }

        .nav-link.active {
            color: var(--accent-color) !important;
        }

        .minecraft-font {
            font-family: 'Minecraft', sans-serif;
        }

        .preview-box {
            background-color: var(--bg-darker);
            border-radius: 8px;
            padding: 1rem;
            text-align: center;
        }

        .preview-box img {
            background-color: #2a2a2a;
            padding: 0.5rem;
            border-radius: 4px;
        }

        .form-label {
            color: var(--text-primary);
        }

        .text-muted {
            color: var(--text-muted) !important;
        }

        footer {
            background-color: var(--bg-darker);
            color: var(--text-secondary);
            padding: 1rem 0;
            text-align: center;
            border-top: 1px solid var(--card-border);
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark mb-4">
        <div class="container">
            <a class="navbar-brand minecraft-font" href="/">
                <i class="bi bi-box"></i> candyskin
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <% if (typeof user !=='undefined' ) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/dashboard">
                                <i class="bi bi-speedometer2"></i> Dashboard
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/profile">
                                <i class="bi bi-person"></i> Profile
                            </a>
                        </li>
                        <li class="nav-item">
                            <form action="/logout" method="POST" class="d-inline">
                                <button type="submit" class="nav-link border-0 bg-transparent">
                                    <i class="bi bi-box-arrow-right"></i> Logout
                                </button>
                            </form>
                        </li>
                        <% } else { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/login">Login</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/register">Register</a>
                            </li>
                            <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mb-4">
        <%- body %>
    </div>

    <footer>
        <div class="container">
            <p>&copy; <%= new Date().getFullYear() %> candyskin by candiedapple. All rights reserved.</p>
            <div class="mt-2">
                <a href="https://discord.gg/luthien" class="text-secondary me-3">
                    <i class="bi bi-discord"></i> Discord
                </a>
                <a href="https://github.com/candied-apple" class="text-secondary">
                    <i class="bi bi-github"></i> GitHub
                </a>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>